using System.Collections;
using ElectricDrill.SimpleRpgCore;
using NUnit.Framework;
using UnityEngine;
using UnityEngine.TestTools;

namespace ElectricDrill.SimpleRpgCore.Tests
{
    public class HealthTests
    {
        /*[Test]
        public void AddingMoreThanMxHealthDoesntTrespassMaxValue() {
            // Arrange
            EntityHealth health = new GameObject().AddComponent<EntityHealth>();

            // Act
            health.AddHealth(health.MAX_HP + 100);

            // Assert
            Assert.AreEqual(health.MAX_HP, health.HP);
        }

        [Test]
        public void RemovingMoreThanMaxHealthWhenHealthCanBeNegativeIsFalseDoesntTrespassZero() {
            // Arrange
            EntityHealth health = new GameObject().AddComponent<EntityHealth>();
            health.HealthCanBeNegative = false;

            // Act
            health.RemoveHealth(health.MAX_HP + 100);

            // Assert
            Assert.AreEqual(0, health.HP);
        }*/
    }
}